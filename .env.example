# Tail MCP — optional server settings
#
# Default: no .env needed. Run: npm run docker:up
#
# Optional hardening — require HMAC on WebSocket messages (server-side only)
# EXTENSION_SECRET=
#
# Optional — override ports inside the container
# EXTENSION_PORT=18789
# MCP_HTTP_PORT=18790
